name: Run Vavoo Scraper

on:
  push:
    branches: [ "main" ] # Veya hangi tetikleyiciyi kullanıyorsanız
  workflow_dispatch: # Manuel çalıştırma için

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Kodu makineye indirir
      - name: Check out repository
        uses: actions/checkout@v4

      # 2. Python'u kurar
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10' # İstediğiniz Python sürümü

      # 3. Bağımlılıkları yükler (Gerekli Adım)
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # requirements.txt dosyanız olduğunu varsayarak

      # 4. Betiğinizi çalıştırır
      - name: Run Vavoo Scraper
        run: python vavoo_scraper.py
        
      # 5. (İsteğe bağlı) Oluşan dosyayı commit'ler
      - name: Commit M3U8 file
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add vavoo_kanallar.m3u8
          # Sadece değişiklik varsa commit at
          git diff-index --quiet HEAD || git commit -m "📺 Kanal listesi güncellendi"
          git push
